<h1>New contacts</h1>

<%= form_for :contact, url: articles_path, data: {turbo:false} do |f| %>
	<p>
		<%= f.label :email %><br />
		<%= f.text_field :title %>
	</p>
	<p>
		<%= f.label :message %><br />
		<%= f.text_area :text %>
	</p>

	<p>
		<%= f.submit %>
	</p>	
<% end %>